#include "monty.h"

/**
 * nop - It does nothing.
 * @stack: Pointer to the  pointer pointing to the top node of my stack.
 * @line_number: the integer representing the line number of opcode.
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
